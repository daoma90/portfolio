import { HeroText } from "@/components/library/atoms/typography";
import React from "react";
import * as s from "./styles";

const GlowCards = () => {
  return (
    <s.Container>
      <HeroText>Glow cards</HeroText>
    </s.Container>
  );
};

export default GlowCards;
